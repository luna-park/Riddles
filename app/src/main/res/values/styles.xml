<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Light.NoTitleBar" />


    <style name="LTFTheme" parent="android:Theme.Holo.Light">
        <item name="android:colorBackground">@color/primary_light</item>
    </style>

</resources>
